# 코드 요청 — 새 플레이어 스프라이트 + 활 레이어

> 대상: `E:\claude\web-game` code 채팅
> 작성 2026-08-18. 이미지·수치는 애니 테스트에서 눈으로 확정한 값이다.

---

## 0. 한 줄 요약

플레이어 캐릭터를 새로 만들었다. **시트가 교체됐고**, `run` 은 프레임 6개에
**재생 순서를 지정**해야 하며, **활이 별도 스프라이트**로 분리됐다.

---

## 1. 교체된 파일 (이미 저장돼 있다)

```
public/sprites/dungeon/deliverables/player_spritesheet.png   교체됨  768×812
public/sprites/dungeon/deliverables/player_bow.png           신규    12×81
```

시트 규격은 그대로다 — 셀 96×116, 8열×7행, 접지 y=108.
현재 채워진 행은 **0(idle 4프레임)** 과 **1(run 6프레임)** 뿐이다.
`back_run` / `attack` / `multishot` / `hit` / `death` 행은 **아직 비어 있다.**

---

## 2. `defs` 수정 — run 이 6프레임이고 순서를 지정한다

`src/main.js:1025` 근처.

### 지금
```js
run: { row: 1, frames: 6, fps: 12, loop: true, skip: 1 },
```

### 이렇게
```js
run: { row: 1, frames: 6, fps: 12, loop: true, order: [0, 2, 3, 5, 3, 2] },
```

`skip: 1` 을 **없앤다.** 새 시트는 0번 열부터 유효하다.

`order` 는 **행 안의 열 번호(0부터)** 다. 애니 테스트에서 눈으로 고른 순서이고,
사람이 세는 번호로는 `1,3,4,6,4,3` 이다.

### 왜 순서를 지정하나
6프레임을 순서대로 돌리면 f2·f5(=열 1·4)에서 **멈칫한다.** 그 둘은 두 발이
모이는 passing 자세라 보폭이 10% 로 떨어지는데, 앞뒤 프레임이 79~80% 라
`79 → 10 → 43` 의 낙차가 뚝뚝 끊겨 보인다. 실측:

```
전체 6프레임   보폭 79 → 10 → 43 → 80 → 10 → 44   인접 최대변화 70
[0,2,3,5,3,2]  보폭 79 → 43 → 80 → 44 → 80 → 43   인접 최대변화 37
```

뒷부분 `3,2` 는 되감기다. 열 1·4 는 이 순서에서 **안 쓰인다.**
시트에는 남겨둔다 — 나중에 8프레임으로 늘릴 때 쓴다.

### `generateFrameNumbers` 는 배열을 받는다
```js
for (const key in defs) {
  if (this.anims.exists(key)) continue
  const d = defs[key]
  const rowStart = d.row * 8
  this.anims.create({
    key,
    frames: d.order
      // ★ 임의 순서 — frames 옵션에 배열을 넘기면 그 순서로 재생된다
      ? this.anims.generateFrameNumbers('archer', {
          frames: d.order.map((c) => rowStart + c),
        })
      : this.anims.generateFrameNumbers('archer', {
          start: rowStart + (d.skip || 0),
          end: rowStart + d.frames - 1,
        }),
    frameRate: d.fps,
    repeat: d.loop ? -1 : 0,
  })
}
```

---

## 3. 활 — 별도 스프라이트, **플레이어 뒤**

### 왜 분리했나
확정 시안이 활을 캐릭터 옆에 별도 오브젝트로 그려왔고 손은 비어 있다.
그대로 가는 게 유리하다.

- 활을 손에 들면 **팔이 스윙을 못 한다.** 이전 시안들이 활을 앞으로 뻗고 있어서
  달리기 자세 자체가 성립하지 않았다
- run 6프레임마다 활을 다시 그릴 필요가 없다 → 형태가 프레임마다 안 흔들린다
- 각도를 코드로 준다. 등에 비스듬히 멘 배치가 프레임마다 자연스럽게 바뀐다

### 로드
```js
this.load.image('playerbow', '/sprites/dungeon/deliverables/player_bow.png')
```

### 생성 — `playerSprite` **보다 먼저** worldLayer 에 넣는다
```js
// ⚠️ 순서가 중요하다. 먼저 add 해야 플레이어 **뒤**에 깔린다(등에 멘 배치).
this.bowSprite = this.add
  .image(this.player.x, this.player.y, 'playerbow')
  .setOrigin(0.5, 0.52)          // 활 중앙보다 살짝 아래가 손잡이
  .setScale(scale)               // 플레이어와 같은 scale
this.worldLayer.add(this.bowSprite)
// … 그다음에 playerSprite 를 만들고 add 한다
```

### 프레임별 오프셋 — **애니메이션마다 다르다**

idle 은 서 있고 run 은 팔이 크게 스윙한다. 손 위치가 달라서 같은 값을 쓰면
idle 에서 활이 몸에서 떨어져 보인다. 애니별로 나눈다.

```js
// [x, y, 각도(도)] × 시트 열.
//   y 는 **발끝(접지선) 기준**, 음수가 위. 파이프라인의 ANCHOR 와 같은 기준이다.
//   x 는 캐릭터 중심 기준, 양수가 앞(바라보는 쪽).
//   전부 애니 테스트에서 프레임마다 눈으로 맞춘 값이다.
const BOW_OFF = {
  idle: [
    [3, -40, 50],
    [6, -40, 50],
    [4, -41, 50],
    [4, -40, 50],
  ],
  run: [
    [-22, -39, 70],   // 열0  CONTACT 오른발
    [ 15, -51, 63],   // 열1  (현재 order 에서 미사용)
    [ 28, -46, 22],   // 열2  PUSH 오른발
    [-28, -36, 50],   // 열3  CONTACT 왼발
    [ 14, -51, 51],   // 열4  (현재 order 에서 미사용)
    [ 28, -46, 20],   // 열5  PUSH 왼발
  ],
}
```

> idle 은 4프레임이라 배열도 4개다. run 6프레임 중 열 1·4 는 `order` 에서
> 안 쓰이지만, **인덱스가 어긋나지 않게 자리를 채워둔다.**

### 매 프레임 갱신

```js
// updatePlayerAnim 뒤나 update 말미에 둔다.
const table = BOW_OFF[this.animKey]
if (table) {
  const col = this.playerSprite.frame.name % 8   // 행 안의 열 번호
  const off = table[col] || table[0]             // 아직 없는 애니는 첫 값으로 버틴다
  const dir = this.playerSprite.scaleX < 0 ? -1 : 1
  this.bowSprite.setPosition(
    this.player.x + off[0] * scale * dir,
    this.player.y + off[1] * scale
  )
  this.bowSprite.setScale(scale * dir, scale)
  this.bowSprite.setAngle(off[2] * dir)
  this.bowSprite.setVisible(this.animKey !== 'death')
} else {
  // back_run / hit / death 는 아직 값이 없다. 활을 감춘다.
  this.bowSprite.setVisible(false)
}
```

> `dir` 을 x·각도에 **둘 다** 곱해야 왼쪽을 볼 때 활이 반대로 눕지 않는다.
> `BOW_OFF` 에 없는 애니는 활을 감춘다 — 엉뚱한 위치에 붙는 것보다 낫다.

## 4. 확인할 것

```
npm run build
```

그리고 게임을 띄워서 눈으로:

1. **달릴 때 활이 손이나 몸에서 떨어져 보이지 않는가** — 오프셋이 어긋나면
   BOW_OFF 값을 조정한다. `tools/sprites/_anim_test.html` 에서 슬라이더로 다시 뽑으면 된다
2. **좌우로 방향을 바꿔도 활이 등 쪽에 붙어 있는가**
3. **run 이 멈칫하지 않는가**
4. 죽을 때 활이 남아 있지 않은가

---

## 5. 아직 안 된 것 — 이 커밋에 넣지 말 것

```
back_run / hit / death        시트 행이 비어 있다. 낱장으로 받는 중
attack / multishot            재생 여부 자체가 미결정 (현재 코드도 호출 안 함)
idle 팔 발광                  현재 idle 은 팔 룬 발광이 약하다. 낱장으로 재요청 예정
```

`death` 가 비어 있으므로 **죽음 연출이 깨질 수 있다.** 확인하고, 깨지면
`death` 재생을 임시로 막거나 이전 시트를 폴백으로 두는 편이 안전하다.

---

## 6. 배경 — 왜 캐릭터를 갈아엎었나

run 애니메이션을 **13번** 실패했다. 원인은 도구도 프롬프트도 아니고
**옛 캐릭터의 망토가 몸 높이의 85%를 덮어 다리가 안 보인 것**이었다.
달리기는 보폭으로 표현되는데 보여줄 다리가 없었다.

새 캐릭터 실측 — 하반신 폭 / 몸통 폭:
```
옛 캐릭터   1.21   ← 로브가 몸통보다도 넓다
새 캐릭터   0.50
```

이번엔 프레임별 보폭(13/31/50/18%)을 숫자로 지정할 수 있었고, 그게 처음이었다.
