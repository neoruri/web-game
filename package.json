{
  "name": "web-game",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "test:ui": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "vite": "^8.1.1"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  }
}
